# P3.3 Approved Design Preview

Enter only with `p3_2_complete`. Generate the representative Style Anchor first. A Raw Generated Layer is never user-approvable by itself.

Map every important visual to `native_shape`, `native_text`, `native_chart`, `sanitized_svg`, `reusable_raster`, `generated_background`, `generated_foreground`, or `decorative_approximation`. Every Critical/Major element needs a P4 Strategy. Raster and Foreground elements must pass provisional extraction before approval. Element Maps describe the image; they never redesign it.

Build Final Previews as Preview-only PowerPoint files containing the Raw Layer plus Native Text/Shape/Chart/SVG and approved Foreground ordering. Render with Microsoft PowerPoint and show that PNG to the user. Bind explicit confirmation to its hash.

After Anchor approval, use the Sanitized Style Reference for the remaining pages. Present the deterministic Contact Sheet and bind final approval. Automated regression must replay frozen Manual Evidence without image calls, Host mapping, Reviewer calls, or human waits.
