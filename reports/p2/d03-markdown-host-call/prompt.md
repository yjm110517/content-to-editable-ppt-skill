# D03 Markdown Wireframe Host Task

Using only the supplied P1 Approved Slide Content, produce one JSON candidate conforming to `markdown-wireframe-candidate.schema.json`.

- Preserve the three Slide IDs and their order.
- Put every Content Ref exactly once in `layout_draft` using the required placeholder syntax.
- Use only approved-text substring labels and controlled visual-zone placeholders.
- Describe hierarchy, relationships, reserved visual areas, and reading order in `layout_notes`.
- Do not add page copy, HTML metadata, SVG, coordinates, images, or final visual styling.
- Do not include hidden reasoning or unrelated conversation context.
